export const metadata = {
  title: 'Your KSubZone Profile',
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function ProfileLayout({ children }) {
  return children;
}
